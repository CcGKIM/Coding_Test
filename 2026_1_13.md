---

# 🐸 Frog Jump Path Finding (BFS + Graph)

개구리가 **시작 지점에서 목표 지점까지**
주어진 시간 제한 안에 **중간 구멍(hole)을 거쳐 이동할 수 있는지**를 판단하는 문제입니다.

---

## 📌 문제 핵심 요약

* 개구리는 속도 `v` (m/s)로 점프할 수 있음
* 한 번 점프할 수 있는 **최대 시간**은 `m`분
* 즉, **한 번에 이동 가능한 최대 거리**는

  ```
  v × (m × 60)
  ```
* 시작 지점, 목표 지점, 여러 중간 구멍 좌표가 주어짐
* 조건을 만족하는 점프들만 이용해서

  * 목표 지점에 도달 가능하면
    👉 `Yes, visiting X other holes.`
  * 불가능하면
    👉 `No.`

---

## 🧠 해결 전략

### 1️⃣ 그래프 모델링

* **각 좌표 = 노드**
* **한 번 점프로 도달 가능하면 간선 연결**

```text
거리 / 속도 < m × 60  →  연결 가능
```

👉 좌표 간의 유클리드 거리를 이용해 그래프를 만듦

---

### 2️⃣ BFS (최단 방문 횟수 탐색)

* BFS를 사용해 **최소 구멍 방문 횟수** 탐색
* 시작점 → 목표점 도달 시 종료
* 목표 지점은 **방문 횟수에 포함하지 않음**

---

## 🧩 전체 코드

```python
from collections import defaultdict, deque

v, m = map(float, input().split())
start_x, start_y = map(float, input().split())
target_x, target_y = map(float, input().split())

arr = []

arr.append((start_x, start_y))

while True:
    try:
        x, y = map(float, input().split())
        arr.append((x, y))
    except:
        break

arr.append((target_x, target_y))

tree = defaultdict(list)

for i in range(len(arr)):
    sx, sy = arr[i]
    for j in range(i + 1, len(arr)):
        tx, ty = arr[j]
        if ((sx - tx) ** 2 + (sy - ty) ** 2) ** 0.5 / v < m * 60:
            tree[(sx, sy)].append((tx, ty))
            tree[(tx, ty)].append((sx, sy))

queue = deque()
visited = set()

queue.append((start_x, start_y, 0))
visited.add((start_x, start_y))

answer = -1

while queue:
    x, y, cnt = queue.popleft()

    if (x, y) == (target_x, target_y):
        answer = cnt
        break

    for nx, ny in tree[(x, y)]:
        if (nx, ny) not in visited:
            if (nx, ny) != (target_x, target_y):
                queue.append((nx, ny, cnt + 1))
            else:
                queue.append((nx, ny, cnt))
            visited.add((nx, ny))

if answer != -1:
    print(f"Yes, visiting {answer} other holes.")
else:
    print("No.")
```

---

## ⚠️ 중요 포인트 정리

### ✅ 왜 BFS인가?

* **최소 구멍 방문 횟수**를 구해야 함
* 모든 간선 비용이 동일 → BFS가 최적

---

### ✅ 목표 지점 방문 시 `cnt` 증가 안 하는 이유

```python
if (nx, ny) != (target_x, target_y):
    cnt + 1
else:
    cnt
```

👉 문제에서 요구하는 건
**“방문한 다른 구멍 수”** 이기 때문

---

### ✅ 그래프 구성 시간복잡도

* 노드 수: `N`
* 모든 쌍 거리 계산 → **O(N²)**
* BFS 탐색 → **O(N + E)**

> 좌표 수가 많지 않다는 전제에서 충분히 통과 가능

---

## 🧪 예시 출력

```text
Yes, visiting 2 other holes.
```

```text
No.
```

---

## 🎯 정리 한 줄

> **좌표를 노드로 보고, 한 번에 도달 가능한 경우만 간선으로 연결한 뒤
> BFS로 최소 구멍 방문 경로를 찾는 문제**

---