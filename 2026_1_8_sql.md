---

## 🐾 특정 이름을 가진 동물 조회하기

### 📌 문제 설명

`ANIMAL_INS` 테이블에서
이름이 **Lucy, Ella, Pickle, Rogan, Sabrina, Mitty** 중 하나인 동물들의 정보를 조회한다.
조회 결과는 **ANIMAL_ID 기준 오름차순**으로 정렬한다.

---

### 🧠 핵심 포인트

* 여러 값을 비교해야 할 때 `IN` 사용
* 특정 컬럼만 선택하여 출력
* 결과 정렬은 `ORDER BY`

---

### ✅ 사용한 SQL 쿼리

```sql
SELECT ANIMAL_ID, NAME, SEX_UPON_INTAKE
FROM ANIMAL_INS
WHERE NAME IN ('Lucy', 'Ella', 'Pickle', 'Rogan', 'Sabrina', 'Mitty')
ORDER BY ANIMAL_ID;
```

---

### 🔍 쿼리 해설

| 구문                    | 설명                    |
| --------------------- | --------------------- |
| `SELECT`              | 출력할 컬럼 선택             |
| `FROM ANIMAL_INS`     | 동물 보호소 입소 정보 테이블      |
| `WHERE NAME IN (...)` | 이름이 특정 목록에 포함된 행만 필터링 |
| `ORDER BY ANIMAL_ID`  | 동물 ID 기준 오름차순 정렬      |

---

### 💡 왜 `IN`을 사용했을까?

```sql
WHERE NAME = 'Lucy'
   OR NAME = 'Ella'
   OR NAME = 'Pickle'
   ...
```

보다

```sql
WHERE NAME IN (...)
```

* 가독성 ✅
* 유지보수 ✅
* 실무에서도 더 선호됨

---

### 🧩 정리

* **여러 조건 값 비교** → `IN`
* **결과 정렬 필수** → `ORDER BY`
* SQL 코딩 테스트에서 **자주 나오는 패턴**

---