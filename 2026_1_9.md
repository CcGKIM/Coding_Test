# 백준 19641번 - 중첩 집합 모델

## 문제 분석
중첩 집합 모델(Nested Set Model)은 계층적 구조를 구간의 포함관계로 표현하는 방식입니다. 트리의 각 노드를 DFS 방문 순서로 left, right 값을 할당하여 구간으로 나타냅니다.

### 핵심 개념
- 각 노드는 `[left, right]` 구간을 가짐
- 부모 노드의 구간이 자식 노드의 구간을 포함
- 자식 노드는 번호가 작은 순서대로 오름차순 방문
- 각 노드의 구간 크기 = 서브트리 노드 개수 × 2

## 알고리즘

### 1단계: 서브트리 크기 계산
```python
def subtree_dfs(node, parent):
    for child in tree[node]:
        if child != parent:
            subtree_cnt[node] += subtree_dfs(child, node)
    return subtree_cnt[node]
```
- 각 노드를 루트로 하는 서브트리의 노드 개수를 계산
- Bottom-up 방식으로 자식의 서브트리 크기를 합산

### 2단계: 구간 분할 및 할당
```python
def divide_dfs(node, parent):
    start = left[node] + 1
    end = right[node] - 1
    
    for child in tree[node]:
        if child != parent:
            child_end = start + 2 * subtree_cnt[child] - 1
            left[child] = start
            right[child] = child_end
            divide_dfs(child, node)
            start = child_end + 1
```
- 부모 노드의 구간 `[left, right]` 내부를 자식들에게 분할
- 각 자식의 구간 크기 = `서브트리 크기 × 2`
- 오름차순으로 정렬된 자식 순서대로 구간 할당

## 시간 복잡도
- 서브트리 크기 계산: O(N)
- 구간 할당: O(N)
- 자식 정렬: O(N log N) (최악의 경우)
- **전체: O(N log N)**

## 공간 복잡度
- 인접 리스트: O(N)
- 보조 배열들: O(N)
- **전체: O(N)**

## 주요 포인트

### 1. 재귀 깊이 제한 해제
```python
sys.setrecursionlimit(3000000)
```
- N ≤ 10^5이므로 최대 깊이 고려하여 충분히 크게 설정

### 2. 양방향 간선 처리
```python
if child != parent:
```
- 입력이 양방향 그래프로 주어지므로 부모 노드 제외 필수

### 3. 자식 노드 정렬
```python
tree[parent].sort()
```
- 문제 조건: 번호가 작은 노드부터 오름차순 방문

### 4. 구간 크기 계산
```python
child_end = start + 2 * subtree_cnt[child] - 1
```
- 노드 개수 n개 → 구간 크기 2n (left, right 각각 한 번씩)

## 예제 분석

### 입력
```
3
2 1 3 -1
3 2 -1
1 2 -1
2
```

### 트리 구조
```
    2 (root)
   / \
  1   3
```

### 실행 과정

**1단계: 서브트리 크기**
- subtree_cnt[1] = 1
- subtree_cnt[3] = 1
- subtree_cnt[2] = 3

**2단계: 구간 할당**
- 노드 2: [1, 6] (루트)
- 노드 1: [2, 3] (첫 번째 자식, 크기 2)
- 노드 3: [4, 5] (두 번째 자식, 크기 2)

### 출력
```
1 2 3
2 1 6
3 4 5
```

## 코드
```python
import sys
sys.setrecursionlimit(3000000)

N = int(input())
tree = [[] for _ in range(N + 1)]
subtree_cnt = [1] * (N + 1)

for _ in range(N):
    arr = list(map(int, input().split()))
    parent = arr[0]
    
    for child in arr[1:]:
        if child == -1:
            break
        tree[parent].append(child)
    
    tree[parent].sort()

root = int(input())

def subtree_dfs(node, parent):
    for child in tree[node]:
        if child != parent:
            subtree_cnt[node] += subtree_dfs(child, node)
    return subtree_cnt[node]

subtree_dfs(root, -1)

left = [-1] * (N + 1)
right = [-1] * (N + 1)

left[root] = 1
right[root] = subtree_cnt[root] * 2

def divide_dfs(node, parent):
    curr_left, curr_right = left[node], right[node]
    start = curr_left + 1
    end = curr_right - 1
    
    if start >= end:
        return
    
    for child in tree[node]:
        if child != parent:
            child_end = start + 2 * subtree_cnt[child] - 1
            
            if child_end > end:
                break
            
            left[child] = start
            right[child] = child_end
            
            divide_dfs(child, node)
            start = child_end + 1

divide_dfs(root, -1)

for i in range(1, N + 1):
    print(i, left[i], right[i])
```

## 관련 개념
- DFS (Depth-First Search)
- 트리 순회
- 구간 트리
- 중첩 집합 모델 (Nested Set Model)