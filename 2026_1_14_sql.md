---

# 🐾 오랜 기간 보호한 동물 (SQL)

## 📌 문제 요약

* `ANIMAL_INS` : 보호소에 들어온 동물
* `ANIMAL_OUTS` : 입양(또는 퇴소)된 동물
* **입양 기록이 없는 동물 중**
* **가장 오래 보호된 동물 3마리**의

  * 이름(`NAME`)
  * 보호 시작 날짜(`DATETIME`)
    를 조회

---

## 💡 핵심 아이디어

* 입양된 동물 = `ANIMAL_OUTS` 에 존재
* **입양되지 않은 동물** =
  👉 `ANIMAL_INS`에는 있지만 `ANIMAL_OUTS`에는 없는 `ANIMAL_ID`
* 오래된 순서 = `DATETIME ASC`
* 상위 3개 = `LIMIT 3`

---

## 🧠 접근 방법

1. `ANIMAL_INS` 전체 기준으로 조회
2. 서브쿼리에서

   * `ANIMAL_INS` ↔ `ANIMAL_OUTS` 를 `INNER JOIN`
   * 입양된 `ANIMAL_ID` 목록 추출
3. 메인 쿼리에서

   * `NOT IN` 으로 입양된 동물 제외
4. 보호 시작일 기준 정렬 후 3개 제한

---

## 🧪 SQL 코드

```sql
SELECT a.NAME, a.DATETIME
FROM ANIMAL_INS a
WHERE a.ANIMAL_ID NOT IN (
    SELECT i.ANIMAL_ID
    FROM ANIMAL_INS i
    INNER JOIN ANIMAL_OUTS o
        ON i.ANIMAL_ID = o.ANIMAL_ID
)
ORDER BY a.DATETIME
LIMIT 3;
```

---

## 🔍 쿼리 구조 해설

### 1️⃣ 서브쿼리: 입양된 동물 찾기

```sql
SELECT i.ANIMAL_ID
FROM ANIMAL_INS i
INNER JOIN ANIMAL_OUTS o
    ON i.ANIMAL_ID = o.ANIMAL_ID
```

* `INNER JOIN` → **양쪽에 모두 존재**
* 즉, **입양 기록이 있는 동물만 추출**

---

### 2️⃣ 메인 쿼리: 입양 안 된 동물 필터링

```sql
WHERE a.ANIMAL_ID NOT IN (서브쿼리)
```

* 입양된 동물 ID 제외
* 결과 = **아직 보호 중인 동물**

---

### 3️⃣ 오래된 순 + 상위 3개

```sql
ORDER BY a.DATETIME
LIMIT 3
```

* 보호 시작일이 오래된 순
* 처음 3마리만 출력

---

## ⚠️ 주의할 점

* ❌ `NOT IN` 서브쿼리에 `NULL`이 섞이면 결과가 꼬일 수 있음
  → 이 문제 데이터에서는 안전
* ❌ `LEFT JOIN + IS NULL` 방식과 혼동 주의

---

## 🔁 대안 풀이 (LEFT JOIN 방식)

```sql
SELECT a.NAME, a.DATETIME
FROM ANIMAL_INS a
LEFT JOIN ANIMAL_OUTS o
    ON a.ANIMAL_ID = o.ANIMAL_ID
WHERE o.ANIMAL_ID IS NULL
ORDER BY a.DATETIME
LIMIT 3;
```

### 언제 이 방식이 더 좋을까?

* `NULL` 안전성 중요할 때
* 실무에서 가독성 더 좋을 때

---

## ⏱ 성능 관점

* 인덱스가 있다면:

  * `ANIMAL_ID` 기준 `JOIN` → 빠름
* 데이터가 커질수록:

  * `LEFT JOIN + IS NULL` 이 더 안정적

---

## 📌 한 줄 요약

> **입양 기록이 없는 동물 = INS에는 있고 OUTS에는 없는 ID
> → 오래된 순으로 3마리 조회**

---