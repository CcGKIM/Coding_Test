---

# 🐄 Best Cow Line (Greedy)

## 📌 문제 개요

N개의 문자가 한 줄씩 주어진다.
매번 **왼쪽 끝** 또는 **오른쪽 끝**에서 하나의 문자를 선택하여 문자열을 만들어갈 수 있다.

### 목표

* 최종적으로 만들어지는 문자열이 **사전순으로 가장 작은 문자열**이 되도록 한다.
* 출력은 **80글자마다 줄바꿈**해야 한다.

---

## 💡 해결 전략 (Greedy)

이 문제는 모든 경우를 탐색하는 DFS가 아닌 **그리디 알고리즘**으로 해결할 수 있다.

### 핵심 아이디어

* 항상 **왼쪽 문자 vs 오른쪽 문자**를 비교
* 더 작은 문자를 선택하면 전체 문자열도 사전순 최소가 된다
* 만약 두 문자가 같다면:

  * 안쪽으로 들어가며 처음으로 다른 문자를 찾는다
  * 그 문자를 기준으로 어느 쪽이 사전순으로 유리한지 판단

> 사전순 비교는 **앞 글자부터** 결정되므로,
> 지금 선택하는 한 글자가 이후 모든 선택보다 중요하다.

---

## 🧠 알고리즘 흐름

1. 문자열의 왼쪽(`left`)과 오른쪽(`right`) 포인터 설정
2. 두 문자를 비교

   * 왼쪽 < 오른쪽 → 왼쪽 선택
   * 오른쪽 < 왼쪽 → 오른쪽 선택
3. 같다면:

   * 안쪽으로 이동하며 비교
   * 처음 다른 위치의 문자로 결정
4. 선택한 문자를 결과에 추가
5. 포인터 이동
6. 모든 문자를 선택할 때까지 반복

---

## ✅ 파이썬 구현 코드

```python
N = int(input())

letter = []
for _ in range(N):
    letter.append(input().strip())

new_letter = []

left = 0
right = N - 1

while left <= right:
    if letter[left] < letter[right]:
        new_letter.append(letter[left])
        left += 1
    elif letter[left] > letter[right]:
        new_letter.append(letter[right])
        right -= 1
    else:
        i, j = left, right
        while i < j and letter[i] == letter[j]:
            i += 1
            j -= 1

        if i >= j or letter[i] <= letter[j]:
            new_letter.append(letter[left])
            left += 1
        else:
            new_letter.append(letter[right])
            right -= 1

result = ''.join(new_letter)

# 출력은 80글자마다 줄바꿈
for i in range(0, len(result), 80):
    print(result[i:i + 80])
```

---

## ⏱️ 시간 & 공간 복잡도

* **시간 복잡도**: `O(N²)`

  * 최악의 경우 매 단계마다 안쪽 비교 수행
* **공간 복잡도**: `O(N)`

  * 결과 문자열 저장용

---

## ⚠️ 주의할 점

* 출력 형식 조건을 반드시 지켜야 한다
  👉 **80글자마다 줄바꿈**
* 문자열 덧셈(`+=`) 대신 리스트에 `append` 후 `join` 사용
  👉 성능 최적화

---

## ✨ 정리

* DFS로 모든 경우를 만드는 문제 ❌
* **그리디 선택이 항상 최적인 문제**
* 사전순 비교의 특성을 정확히 이해하는 것이 핵심

---