---

# 💰 Coin Change (동전 교환 – 경우의 수)

주어진 동전 종류를 사용하여 **목표 금액을 만드는 서로 다른 경우의 수**를 구하는 문제입니다.
각 동전은 **무한히 사용할 수 있으며**, 동전의 **순서는 고려하지 않습니다**.

---

## 📌 문제 설명

* 테스트 케이스 수 `T`가 주어진다.
* 각 테스트 케이스마다:

  * 동전의 개수 `N`
  * `N`개의 동전 종류
  * 목표 금액 `M`
* 주어진 동전들로 금액 `M`을 만드는 **경우의 수**를 출력한다.

---

## ✨ 핵심 아이디어 (Dynamic Programming)

### DP 정의

`dp[i]` = **금액 `i`를 만드는 경우의 수**

### 초기값

```python
dp[0] = 1
```

👉 아무 동전도 사용하지 않고 `0원`을 만드는 방법은 **1가지**

---

## 🔄 점화식

각 동전에 대해:

```python
dp[i] += dp[i - coin]
```

의미:

* 현재 동전 `coin`을 사용해서
* `(i - coin)`을 만드는 모든 경우 뒤에
* `coin`을 하나 추가하는 방식

---

## ⚠️ 중요한 포인트 (순서가 중요한 이유)

```python
for coin in coins:
    for i in range(coin, M + 1):
        dp[i] += dp[i - coin]
```

* **동전을 먼저 순회** → **중복 제거**
* 순서를 바꾸면 **순열 문제가 되어 오답**

예:

* (1, 2) 와 (2, 1)은 **같은 경우**
* 따라서 동전 기준으로 DP를 갱신해야 함

---

## 🧠 예시 설명

### 입력

```
coins = [1, 2]
M = 3
```

### 가능한 경우

* 1 + 1 + 1
* 1 + 2

👉 총 **2가지**

---

## 🧪 파이썬 코드

```python
T = int(input())

for _ in range(T):
    N = int(input())
    coins = list(map(int, input().split()))
    M = int(input())

    dp = [0] * (M + 1)
    dp[0] = 1

    for coin in coins:
        for i in range(coin, M + 1):
            dp[i] += dp[i - coin]

    print(dp[M])
```

---

## ⏱ 시간 & 공간 복잡도

| 항목     | 복잡도        |
| ------ | ---------- |
| 시간 복잡도 | `O(N × M)` |
| 공간 복잡도 | `O(M)`     |

* `N`: 동전 개수
* `M`: 목표 금액

---

## 🚀 확장 & 응용

* **최소 동전 개수** → `dp[i] = min(dp[i], dp[i-coin] + 1)`
* **동전 사용 제한 있음** → 배낭 문제(Knapsack)
* **순서 고려 문제** → DP 순서 변경

---

## 📎 정리 한 줄 요약

> 이 문제는 **"동전 기준으로 누적하며 경우의 수를 세는 1차원 DP"**의 대표적인 예제다.

---