---

# 📐 최대 부분 직사각형 합 (2D Prefix Sum)

## 📌 문제 개요

* `N × M` 크기의 2차원 배열이 주어진다
* 임의의 직사각형 영역을 선택할 수 있다
* **그 직사각형 내부 원소들의 합의 최댓값**을 구하라

---

## 💡 핵심 아이디어

* 모든 직사각형을 직접 합으로 계산하면 **너무 느림**
* 👉 **2차원 누적합(Prefix Sum)** 을 사용하면
  **임의의 직사각형 합을 O(1)** 에 계산 가능
* 이후:

  * 모든 `(i1, j1) ~ (i2, j2)` 조합을 탐색
  * 누적합으로 빠르게 값 계산
  * 최댓값 갱신

---

## 🧠 접근 전략

### 1️⃣ 2차원 누적합 배열 생성

```text
prefix[i][j] =
    (1,1)부터 (i,j)까지의 모든 합
```

계산식:

```
prefix[i][j] =
    board[i-1][j-1]
  + prefix[i-1][j]
  + prefix[i][j-1]
  - prefix[i-1][j-1]
```

---

### 2️⃣ 직사각형 합 O(1) 계산

직사각형 `(i1, j1) ~ (i2, j2)` 합:

```
prefix[i2][j2]
- prefix[i2][j1-1]
- prefix[i1-1][j2]
+ prefix[i1-1][j1-1]
```

---

### 3️⃣ 모든 직사각형 탐색

* 시작 좌표 `(i1, j1)`
* 끝 좌표 `(i2, j2)`
* 총 경우의 수: `O(N²M²)`
* 각 계산은 `O(1)`

---

## 🧪 파이썬 코드

```python
N, M = map(int, input().split())

board = []
prefix = [[0] * (M + 1) for _ in range(N + 1)]

for _ in range(N):
    arr = list(map(int, input().split()))
    board.append(arr)

# 2차원 누적합 생성
for i in range(1, N + 1):
    for j in range(1, M + 1):
        prefix[i][j] = (
            board[i - 1][j - 1]
            + prefix[i - 1][j]
            + prefix[i][j - 1]
            - prefix[i - 1][j - 1]
        )

def prefix_sum(i1, j1, i2, j2):
    return (
        prefix[i2][j2]
        - prefix[i2][j1 - 1]
        - prefix[i1 - 1][j2]
        + prefix[i1 - 1][j1 - 1]
    )

answer = -float('inf')

for i1 in range(1, N + 1):
    for j1 in range(1, M + 1):
        for i2 in range(i1, N + 1):
            for j2 in range(j1, M + 1):
                answer = max(answer, prefix_sum(i1, j1, i2, j2))

print(answer)
```

---

## ⏱ 시간 복잡도 분석

| 단계      | 복잡도         |
| ------- | ----------- |
| 누적합 생성  | O(NM)       |
| 직사각형 탐색 | O(N²M²)     |
| 합 계산    | O(1)        |
| **총합**  | **O(N²M²)** |

---

## ❗ 주의할 점

* `prefix` 배열을 **1-indexed** 로 만들지 않으면 인덱스 지옥
* 초기 `answer` 값은 반드시 **음의 무한대**

  * 모든 값이 음수일 수 있음
* 범위 계산에서 `- prefix[i1-1][j1-1]` 꼭 포함

---

## 🔁 더 좋은 풀이가 있는 경우

### 🔥 Kadane + 행 압축 (고급)

* 시간 복잡도: **O(N²M)**
* 행을 고정 → 열 누적 → 1차원 최대 부분합
* 이 문제를 **대형 입력**으로 확장할 때 필수

(이건 다른 문제에서 매우 강력함)

---

## 📌 한 줄 요약

> **2차원 누적합으로 직사각형 합을 O(1)에 계산하고,
> 모든 영역을 탐색해 최댓값을 구한다**

---