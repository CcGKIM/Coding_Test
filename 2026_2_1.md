---

# 🔥 마법사 상어와 파이어볼 (Simulation)

## 📌 문제 요약

* `N x N` 격자 위에 여러 개의 **파이어볼**이 존재
* 각 파이어볼은 `(질량 m, 속력 s, 방향 d)`를 가짐
* 총 `K`번의 명령을 수행하며, 매 턴마다:

  1. 모든 파이어볼 이동
  2. 같은 칸에 모인 파이어볼 병합/분리 규칙 적용
* **최종적으로 남은 파이어볼 질량의 합**을 출력

---

## 💡 핵심 아이디어

### 1️⃣ 좌표별 파이어볼 관리

- 격자 전체를 매번 순회하지 않고
- **파이어볼이 존재하는 칸만 관리** → `defaultdict(list)` 활용

```python
f_ball_loc[(r, c)] = [(m, d, s), ...]
```

➡️ **희소(sparse)한 상태 관리**로 불필요한 연산 제거

---

### 2️⃣ 방향 벡터 (8방향 이동)

```python
dr = (-1, -1, 0, 1, 1, 1, 0, -1)
dc = (0, 1, 1, 1, 0, -1, -1, -1)
```

- `d ∈ [0..7]`
- `(dr[d], dc[d]) * s` 만큼 이동
- 격자 밖으로 나가면 **모듈러 연산으로 순환**

```python
nr = (r + dr[d] * s) % N
nc = (c + dc[d] * s) % N
```

---

## 🔁 시뮬레이션 흐름

### 🔹 Step 1. 파이어볼 이동

```python
new_loc = defaultdict(list)

for (r, c), balls in f_ball_loc.items():
    for m, d, s in balls:
        nr = (r + dr[d] * s) % N
        nc = (c + dc[d] * s) % N
        new_loc[(nr, nc)].append((m, d, s))
```

- 모든 파이어볼을 **동시에 이동**
- 이동 결과를 `new_loc`에 저장

---

### 🔹 Step 2. 병합 & 분리

#### ✅ 같은 칸에 2개 이상 있을 때

1. 질량 합 `M`, 속력 합 `S`
2. 새 질량 = `M // 5`
   - 0이면 **전부 소멸**

3. 새 속력 = `S // 개수`
4. 방향 결정
   - 모두 짝수 or 모두 홀수 → `0,2,4,6`
   - 아니면 → `1,3,5,7`

```python
if len(value) >= 2:
    M, S = 0, 0
    for m, _, s in value:
        M += m
        S += s

    if M // 5 == 0:
        continue

    if all(v[1] % 2 == 0 for v in value) or all(v[1] % 2 != 0 for v in value):
        dirs = (0, 2, 4, 6)
    else:
        dirs = (1, 3, 5, 7)

    for d in dirs:
        new_loc[key].append((M // 5, d, S // len(value)))
```

---

#### ✅ 하나만 있는 경우

- 그대로 유지

```python
else:
    new_loc[key].append(value[0])
```

---

## 🧮 최종 결과 계산

```python
answer = 0
for balls in f_ball_loc.values():
    for m, _, _ in balls:
        answer += m

print(answer)
```

---

## ⏱️ 시간 복잡도 분석

- `K`번 반복
- 매 턴마다 **존재하는 파이어볼 개수만큼 연산**
- 최대 파이어볼 수는 제한되어 있으므로

➡️ **O(K × M)**
(문제 조건 내에서 충분히 통과)

---

## ✅ 이 풀이의 장점

- ❌ 전체 보드 순회 없음
- ✅ 실제 파이어볼 위치만 관리
- ✅ 규칙 분기 명확
- ✅ 구현 실수 방지 (모듈러 이동, 방향 체크)

---
