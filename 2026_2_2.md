---

# 🌳 트리 이동 최소 비용 문제 (DFS)

## 📌 문제 개요

* `N`개의 노드로 이루어진 **가중치 트리**
* 시작 노드 `S`에서 출발
* **모든 간선을 최소 한 번 이상 방문**해야 함
* 이동 비용은 간선의 가중치
* **최소 이동 비용**을 구하는 문제

---

## 💡 핵심 아이디어 한 줄 요약

> **모든 간선을 왕복하면 비용은 `2 × 전체 간선 합`,
> 단 한 번만 안 돌아와도 되는 가장 먼 경로를 빼주면 된다.**

---

## 🧠 사고 과정

### 1️⃣ 기본 전략 (무식한 방법)

* 모든 간선을 **갔다가 다시 돌아오기**
* 👉 총 비용 = `2 × (모든 간선 가중치 합)`

하지만 ❗

* 마지막에 **출발점으로 돌아올 필요는 없음**

---

### 2️⃣ 최적화 포인트

* 시작점 `S`에서 **가장 멀리 있는 노드까지의 거리**
* 이 경로는 **왕복할 필요 없이 편도만 이동**

👉 따라서 최종 비용은:

[
\textbf{최소 비용} = 2 \times (\text{전체 간선 비용}) - (\text{S에서 가장 먼 거리})
]

---

## 🔧 구현 방법

### 📍 트리 구성

```python
tree = [[] for _ in range(N + 1)]
total_cost = 0

for _ in range(N - 1):
    a, b, c = map(int, input().split())
    tree[a].append((b, c))
    tree[b].append((a, c))
    total_cost += c
```

* 인접 리스트로 트리 저장
* 간선 입력 시 **전체 비용 누적**

---

### 📍 DFS로 가장 먼 거리 찾기

```python
visited = [False] * (N + 1)
visited[S] = True

def dfs(x):
    max_dist = 0
    for child, cost in tree[x]:
        if not visited[child]:
            visited[child] = True
            max_dist = max(max_dist, dfs(child) + cost)
    return max_dist
```

* `S`에서 출발
* 리프까지의 **최대 거리** 계산
* 트리는 사이클이 없으므로 DFS 안전

---

### 📍 최종 계산

```python
max_distance = dfs(S)
print(2 * total_cost - max_distance)
```

---

## ⏱️ 시간 복잡도

* DFS 한 번
* **O(N)**

공간 복잡도 역시 **O(N)**

---

## ✅ 이 풀이의 장점

* ✔️ 구현이 단순하고 직관적
* ✔️ DFS 한 번으로 해결
* ✔️ 불필요한 DP, 다익스트라 필요 없음
* ✔️ 트리 문제에서 자주 쓰이는 패턴

---

## ⚠️ 주의할 점 (실수 포인트)

* 재귀 깊이 → `sys.setrecursionlimit` 필수
* 시작 노드 `S`에서 DFS 시작해야 함
* `visited` 체크 안 하면 무한 루프

---

## 🧠 유형 정리

* **트리**
* **DFS**
* **그리디 사고**
* **“전체 왕복 - 최장 편도” 패턴**

---

## ✨ 한 줄 정리

> **트리에서 모든 간선을 방문해야 할 때,
> 최소 비용은 `2 × 전체 간선 비용 - 시작점 기준 최장 거리`다.**

---