---

# 🧮 Dynamic Programming – 상태 전이 기반 카운팅 문제

## 📌 문제 개요

정수 `N`이 주어질 때,
총 `2N`일 동안 특정 규칙에 따라 상태가 변화하며
**마지막 날에 `(i=0, j=0)` 상태로 도달하는 모든 경우의 수**를 구하는 문제이다.

입력은 여러 개의 `N`이 주어지며, `N ≤ 0`이 입력되면 종료한다.

---

## 💡 핵심 아이디어

이 문제는 **시간(day)에 따라 상태가 변화하는 과정**을 모두 세는 문제로,
👉 **완전 탐색은 불가능**하고
👉 **동적 계획법(DP)** 을 사용해야 한다.

---

## 🧠 DP 상태 정의

```text
dp[day][i][j]
```

* `day` : 현재 날짜 (0 ~ 2N)
* `i` : 첫 번째 자원의 개수 (0 ~ N)
* `j` : 두 번째 자원의 개수 (0 ~ N)
* 값 의미
  👉 `day`일에 `(i, j)` 상태에 도달하는 **경우의 수**

---

## 🎯 초기 상태

```python
dp[0][N][0] = 1
```

* 0일차에는

  * 첫 번째 자원 `N`개
  * 두 번째 자원 `0`개
    상태에서 시작

---

## 🔄 상태 전이 (Transition)

각 날짜마다 가능한 행동은 **두 가지**이다.

### 1️⃣ 첫 번째 자원을 하나 사용 → 두 번째 자원 증가

```text
(i, j) → (i-1, j+1)
```

조건:

* `i > 0`
* `j + 1 ≤ N`

```python
dp[day + 1][i - 1][j + 1] += dp[day][i][j]
```

---

### 2️⃣ 두 번째 자원을 하나 사용

```text
(i, j) → (i, j-1)
```

조건:

* `j > 0`

```python
dp[day + 1][i][j - 1] += dp[day][i][j]
```

---

## 🏁 정답 조건

```python
dp[2 * N][0][0]
```

* `2N`일이 모두 지난 후
* 두 자원이 모두 0이 되는 경우의 수

---

## 🧪 전체 코드

```python
def solve_dp(N):
    # dp[day][i][j]
    dp = [[[0] * (N + 1) for _ in range(N + 1)] for _ in range(2 * N + 1)]

    # 초기 상태
    dp[0][N][0] = 1

    for day in range(2 * N):
        for i in range(N + 1):
            for j in range(N + 1):
                if dp[day][i][j]:
                    # 첫 번째 자원 사용
                    if i > 0 and j + 1 <= N:
                        dp[day + 1][i - 1][j + 1] += dp[day][i][j]

                    # 두 번째 자원 사용
                    if j > 0:
                        dp[day + 1][i][j - 1] += dp[day][i][j]

    print(dp[2 * N][0][0])


while True:
    x = int(input())
    if x > 0:
        solve_dp(x)
    else:
        break
```

---

## ⏱ 시간 & 공간 복잡도

### ⏳ 시간 복잡도

```text
O(2N × N × N) = O(N³)
```

### 💾 공간 복잡도

```text
O(2N × N × N) = O(N³)
```

> ⚠️ `N`이 큰 경우 메모리 사용량이 상당히 크므로
> → **2차원 DP로 최적화 가능**

---

## 🔧 최적화 아이디어 (중요 ⭐)

현재 DP는 `day` 축이 불필요하게 크다.
👉 **이전 day만 있으면 다음 day를 계산 가능**

### 👉 Rolling Array 최적화

```text
dp[2][N+1][N+1]
```

* `day % 2`로 현재/다음 상태만 유지
* 공간 복잡도: `O(N²)`로 감소

---

## 📚 정리 요약

* 이 문제는 **상태 기반 DP + 경우의 수 카운팅**
* 핵심은:

  * 상태 정의
  * 전이 조건의 정확성
  * 경계 체크
* 비슷한 문제:

  * 알약 문제
  * 괄호 생성
  * 자원 소비 시뮬레이션 DP

---