---

# 🏹 사냥꾼 (Binary Search 활용)

## 📌 문제 요약

* 사대가 일직선 상에 여러 개 존재
* 각 동물은 `(x, y)` 좌표에 위치
* 사정거리 `L` 안에 있는 동물만 사냥 가능
* 거리 계산:
  [
  |사대_x - 동물_x| + 동물_y \le L
  ]

👉 **사냥 가능한 동물의 수를 구하는 문제**

---

## 💡 핵심 아이디어

* 사대 위치는 **x축 위**에만 있음
* 동물의 `x` 좌표 기준으로 **가장 가까운 사대만 확인**하면 됨
* 사대를 **정렬**한 뒤
  👉 `bisect_left`, `bisect_right` 로 주변 후보만 탐색

✔️ 전부 비교하면 시간 초과
✔️ 이분 탐색으로 **O(log M)** 만에 해결

---

## 🧠 접근 방법

1. 사대 위치 배열 정렬
2. 각 동물 `(x, y)` 에 대해:

   * `bisect_left(arr, x)`
   * `bisect_right(arr, x)`
3. 해당 인덱스 주변(최대 4개)만 확인
4. 최소 거리 계산 후 `L` 이하면 카운트 증가

---

## 🧪 파이썬 코드

```python
from bisect import bisect_left, bisect_right

M, N, L = map(int, input().split())
arr = list(map(int, input().split()))

answer = 0
arr.sort()

for _ in range(N):
    x, y = map(int, input().split())

    left_idx = bisect_left(arr, x)
    right_idx = bisect_right(arr, x)

    candidates = [
        left_idx - 1,
        left_idx,
        right_idx,
        right_idx + 1
    ]

    min_dist = float('inf')

    for idx in candidates:
        if 0 <= idx < M:
            min_dist = min(min_dist, abs(x - arr[idx]) + y)

    if min_dist <= L:
        answer += 1

print(answer)
```

---

## 🔍 왜 주변 사대만 보면 될까?

사대가 정렬되어 있기 때문에
동물의 `x` 좌표 기준으로:

```
... |----|----|----|----|
         x
```

👉 가장 가까운 사대는

* `x` 바로 왼쪽
* `x` 바로 오른쪽

이 둘만 보면 충분함
(그 외는 거리 더 멀어짐)

---

## ⏱ 시간 복잡도

| 단계       | 복잡도                  |
| -------- | -------------------- |
| 사대 정렬    | O(M log M)           |
| 동물 N개 탐색 | O(N log M)           |
| **총합**   | **O((M + N) log M)** |

✔️ 완전탐색 O(M×N) 대비 대폭 개선

---

## ❗ 자주 실수하는 포인트

* ❌ 모든 사대를 검사 → 시간 초과
* ❌ y 좌표 무시 → 오답
* ❌ bisect 결과 인덱스 범위 체크 안 함

---

## 📌 한 줄 요약

> **사대는 정렬 + 이분 탐색, 동물마다 가장 가까운 사대만 확인하면 끝**

---